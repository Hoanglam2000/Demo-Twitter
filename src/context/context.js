import { createContext } from "react";


export const TweetsContex = createContext(null)
export const HashTagContex = createContext(null)
export const UserFollowerContex = createContext(null)
export const DataContext = createContext(null)

